name: "Issue and PR stale management"
on:
  schedule:
  - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write
  contents: read

jobs:
  stale:
    uses: vyos/.github/.github/workflows/check-stale.yml@production
    secrets: inherit
