#
# MACVLAN / MACVTAP
#
# Sub-interfaces with their own MAC address used by VyOS
# "interfaces pseudo-ethernet" (MACVLAN). MACVTAP exposes the
# same interface as a /dev/tap character device for hypervisors.
#

CONFIG_MACVLAN=m
CONFIG_MACVTAP=m
