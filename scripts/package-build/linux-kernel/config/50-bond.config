#
# Bonding (link aggregation)
#
# Linux bonding driver. Provides 802.3ad LACP, active-backup,
# round-robin and other modes used by VyOS "interfaces bonding".
# Hardware-specific NIC drivers are kept in the per-arch defconfig.
#

CONFIG_BONDING=m
