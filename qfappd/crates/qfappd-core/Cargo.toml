[package]
name = "qfappd-core"
description = "Pure-logic core of qfappd: policy engine, ct-mark codec, flow table, nft rendering, event schema. No OS dependencies — tests run on any platform."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
thiserror.workspace = true
tracing.workspace = true
lru = "0.12"

[dev-dependencies]
tempfile = "3"
